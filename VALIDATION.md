# 验证状态

当前稳定基线：3.7.8。

- 普通 Python 单元测试：64 项（包含兼容性、路径边界、目录命名回退、默认工程位置、原版打开预设行为、BS 同拓扑传递、GN 安装及重载顺序）。
- 单文件可由 scripts/build_single.py 重建；包含全部运行模块。
- 之前已在 Maya 2022 独立进程验证项目同步、无项目预览保护与 Arnold 眼球设置。
- 新增的表达式速查窗口尚未完成真实 Maya 的视觉验收，笔记未逐条做最终毛发渲染验证。
- 测试通过不等同于所有第三方素材完整：旧 V-Ray 素材仍可能引用缺失外部贴图，需项目检查。
- 3.8.0 的预设导入实验已放弃；不属于当前稳定版本。
- 3.7.5 的 BS 改动尚未在真实 Maya 中验收，请运行 `tests/maya_smoke.py` 的 BS 用例确认。
- 3.7.6 的 GN 安装在临时目录做了完整复制与 userSetup 追加测试；真实 Maya / ZBrush 的安装结果需点击按钮后人工确认。
- 3.7.7 已在实际素材库删除重复的 Maya_* 旧目录；如果换到未迁移的旧素材库，靠 `config.resolve_folder` 回退识别，该回退仅有单元测试覆盖。
- 3.7.8 已清理 Arnold 素材中的 V-Ray 残留节点（备份在素材库 `_Backups`），并把默认预览工程移出素材库；首次打开预设会在 Maya 用户目录重建工程并重新同步贴图，需人工确认一次。

本次仓库整理不修改素材库，也不在用户工作场景执行测试。

## 3.7.9 MetaHuman

普通 Python 64 项回归测试通过，单文件构建通过。独立 Maya 2022 mayapy 与临时 MAYA_APP_DIR 实测 tests/maya_metahuman.py 通过：男女及同型号重复导入、已有模型材质不受影响、项目内贴图路径、重复应用不增加 file 节点、逐面材质恢复、还原撤销/重做、改名及保存重开后的恢复。
FBX 原生导入不能完整撤销；材质操作可撤销。未进行交互视口截图或最终渲染验证。

## 3.7.10

独立 Maya 2022 实测观察贴图双向切换、任意头部命名、切 UV 后逐项精确恢复 UV 坐标及面 UV 编号、撤销/重做、保存重开、重复切换以及拒绝错误拓扑全部通过。原有 MetaHuman 导入和材质回归亦通过。

## 3.7.11

独立 Maya 2022 实测校验还原后所有 shadingEngine 连接与原始材质集合一致（含逐面材质）；兼容旧版还原残留场景重新应用/还原。软选择开启与关闭时 UV 结果相同，异常回滚后也恢复软选择，保留距离设置。

## 3.7.12

64 项普通 Python 回归测试通过；8 项独立 Maya 2022 场景测试通过。生长体更新以测试平面验证任意来源名称、UV 传递位置、重复更新、来源历史保留、软选择恢复及撤销；尚未对完整商业素材执行最终外观验证。

## 3.7.13

独立 Maya 2022 使用实际男女 MetaHuman 素材完成原有导入/材质/UV 回归。头身接缝在男性模型上匹配 92 对顶点、约 0.25 秒；验证旋转与非均匀缩放后面顶点法线方向一致，顶点位置未改变，撤销恢复原法线，重做成功。此耗时为本机样本，不代表所有场景。

## 3.7.14

新增 tests/maya_mh_fit.py，使用配套 FBX 构造顶点顺序反转但 UV 未变的头部，验证修复后完整编号/造型、源模型保持、缩放平移后的八个配件位置、旋转适配、重复更新不复制、保存重开后关联保留、UV 改动拒绝及撤销/重做。
测试不代表任意极端雕刻下的眼睑和口腔贴合质量，实际制作仍需目视检查。

3.7.14 补充：男女素材适配流程均通过；男性眼周局部造型修改后，眼球位置随对应区域更新。
